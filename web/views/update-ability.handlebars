<!DOCTYPE HTML>
<p></p>
<h1> {{title}}: {{abilityName}} </h1>

<h5><a href="/">Go back to the home page</a></h5>

<table class="table is-bordered is-striped is-fullwidth table-striped">
    <thead class="thead-dark"> 
        <th>Type</th> 
    </thead> 
    <tbody> 
        {{#each results}} 
        <tr> 
            <td>{{this.type}}</td> 
        </tr> 
        {{/each}} 
    </tbody> 
</table> 

<hr>

<h3>Update {{abilityName}}:</h3>
<form id="update-ability" action="/ability" method="post">
    Ability type: <input type="text" name="type" value="{{type}}"><br>
    <div class="field">
            <input class="button is-info" type="submit" value="Submit">
    </div>
</form>